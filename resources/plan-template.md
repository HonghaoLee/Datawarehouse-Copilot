# 执行计划模板

> plan.md 是规划层文档，面向用户可读，聚焦「做什么」「按什么顺序」「依赖哪些规范」「风险在哪里」。
> 代码级验收标准（DoD）在 task.md 中定义，此处不重复。

---

## ⚙️ 生成前置检查

- [ ] spec.md 已获用户显式批准（Phase 3 确认点已通过）
- [ ] 已读取 `resources/conventions/project-conventions.md`（文件命名、目录结构）
- [ ] 已读取 `resources/conventions/platform-conventions.md`（Azkaban job 类型、Spark 资源参数档位）

---

# 执行计划：{需求名称}

**关联 Spec**：spec.md（已批准）
**状态**：🔵 Draft
**预估工时**：{X} 人天

> 技术架构、分层决策、存储方案均以 spec.md §[对应章节] 为准，此处不重复定义。

---

## 1. 调度方案

调度工具：Azkaban
调度时间：{cron 或时间描述}
超时告警：{N} 小时
失败重试：{N} 次，间隔 {N} 分钟

Flow 依赖关系（DAG 拓扑，描述各 job 间的上下游顺序）：

```
flow: {flow_name}
├── job: {job_1}      # {说明}
├── job: {job_2}      # {说明}，depends: {job_1}
└── job: {job_3}      # {说明}，depends: {job_2}
```

> job 类型、command 模板、Spark 资源参数档位均从 `platform-conventions.md` 中取值，按 spec.md 中对应表/ETL 的规模分级选取。

---

## 2. 任务清单

> 按 spec.md 中每张表、每条 ETL 链路、每个指标逐一拆解，粒度细化到**单文件/单脚本**级别。每行须完整填写 conventions 应用决策，作为 task 生成的直接输入。

| 任务 ID | 描述 | spec 条目 | 输入依赖 | conventions 应用决策 | 产物路径 | 预估工时 |
|---------|------|-----------|----------|----------------------|----------|----------|
| T-01 | {按 spec 表/ETL/指标条目填写} | spec.md §{章节} | {上游表或任务} | {命名规则/存储格式/参数档位/分区策略，如：表名前缀 dwd_，ORC+SNAPPY，executor×10/4g，按 p_date 分区} | {严格按命名规范填写} | {X}h |
| T-02 | … | spec.md §{章节} | … | … | … | … |

> 📌 生成时：将 spec.md 中每张确认表、每条 ETL 链路、每个指标各映射为一行任务，不合并、不遗漏。

---

## 3. 风险与缓解

> 按以下四类分组填写，每条包含触发条件、影响范围、缓解方案、确认节点。若某类无风险则填「无」。

### 数据质量

| 风险描述 | 触发条件 | 影响范围 | 缓解方案 | 确认节点 |
|----------|----------|----------|----------|----------|
| {描述} | {触发条件} | {影响范围} | {缓解措施} | {需哪一方确认后才继续} |

### 调度依赖

| 风险描述 | 触发条件 | 影响范围 | 缓解方案 | 确认节点 |
|----------|----------|----------|----------|----------|
| {描述} | {触发条件} | {影响范围} | {缓解措施} | {需哪一方确认后才继续} |

### 平台限制

| 风险描述 | 触发条件 | 影响范围 | 缓解方案 | 确认节点 |
|----------|----------|----------|----------|----------|
| {描述} | {触发条件} | {影响范围} | {缓解措施} | {需哪一方确认后才继续} |

### 业务逻辑歧义

| 风险描述 | 触发条件 | 影响范围 | 缓解方案 | 确认节点 |
|----------|----------|----------|----------|----------|
| {描述} | {触发条件} | {影响范围} | {缓解措施} | {需哪一方确认后才继续} |

---

## 4. 待确认事项

> 仅记录 spec.md 中遗留的**未决问题**（如字段语义存疑、来源表尚未确认）。若无遗留问题，填写「无」。

- [ ] {问题描述，需标注来源：如「spec.md §2.3 字段 xxx 语义待业务侧确认」}
