# Spec 文档模板

> `[必填]` 项不可省略；`[可选]` 项无相关内容时删除该节；`[待确认]` 表示信息待用户补充。

---

# Spec：{需求名称}

**版本**：v1.0  
**状态**：草稿 / 待确认 / 已确认  
**创建时间**：{date}  
**负责人**：{owner}

---

## 1. 需求背景 [必填]

### 1.1 业务背景

### 1.2 需求目标
- 目标 1：
- 目标 2：

### 1.3 需求范围
**包含**：
-

**不包含（Out of Scope）**：
-

---

## 2. 数据分层与定位 [必填]

| 属性 | 值 |
|------|----|
| 数据层级 | ODS / DWD / DWS / ADS / DIM |
| 所属数据域 | {domain} |
| 所属主题 | {subject} |
| 目标数据库 | {database_name} |
| 目标表名 | {table_name} |

---

## 3. 数据源 [必填]

### 3.1 上游依赖表

| 源表 | 数据库 | 层级 | 关联字段 | 说明 |
|------|--------|------|----------|------|
| {source_table_1} | {db} | ODS | {join_key} | {comment} |

### 3.2 源系统信息 [可选]

| 源系统 | 类型 | 接入方式 | 接入频率 |
|--------|------|----------|----------|
| {system_name} | MySQL / Oracle / Kafka / 埋点 | DataBus / Sqoop / 直连 | 实时 / 每小时 / 每日 |

---

## 4. 目标表设计 [必填]

### 4.1 表基本信息

| 属性 | 值 |
|------|----|
| 表名 | {table_name} |
| 表注释 | {table_comment} |
| 存储格式 | ORC / Parquet |
| 压缩格式 | Snappy / Zlib |
| 数据生命周期 | {N} 天 / 永久 |

### 4.2 字段设计

| 字段名 | 类型 | 是否可空 | 默认值 | 业务含义 | 来源字段 | 转换规则 |
|--------|------|----------|--------|----------|----------|----------|
| {col_1} | STRING | N | - | {业务说明} | {source_col} | 直接映射 / {转换逻辑} |
| dt | STRING | N | - | 数据日期分区，格式 yyyyMMdd | 调度参数 | - |

### 4.3 分区设计

| 分区字段 | 类型 | 说明 | 分区策略 |
|----------|------|------|----------|
| dt | STRING | 日期分区，格式 yyyyMMdd | 按天 |

### 4.4 主键与唯一性约束

- 业务唯一键：{col_a} + {col_b}
- 说明：{唯一性的业务含义}

---

## 5. ETL 逻辑 [必填]

### 5.1 更新策略

| 属性 | 值 |
|------|----|
| 更新方式 | 全量覆盖 / 增量追加 / Upsert |
| 调度周期 | 每日 / 每小时 / 实时 |
| 调度时间 | {cron 或时间描述} |
| 数据时效要求 | T+{N} |

### 5.2 核心转换逻辑

**Step 1**：{步骤描述}
```sql
{伪代码或 SQL 片段}
```

**Step 2**：{步骤描述}
```sql
{伪代码或 SQL 片段}
```

### 5.3 关联关系

| 关联表 | 关联类型 | 关联键 | 说明 |
|--------|----------|--------|------|
| {table} | LEFT JOIN / INNER JOIN | {keys} | {reason} |

### 5.4 过滤条件

| 条件 | 说明 |
|------|------|
| {condition_1} | {业务含义} |

---

## 6. 数据质量规则 [必填]

| 规则 ID | 检查维度 | 检查规则 | 检查 SQL | 阈值 | 异常处理 |
|---------|----------|----------|----------|------|----------|
| DQ-001 | 完整性 | 主键不为空 | `SELECT COUNT(*) FROM {table} WHERE {pk} IS NULL` | = 0 | 告警 / 阻断 |
| DQ-002 | 唯一性 | 主键无重复 | `SELECT COUNT(*)-COUNT(DISTINCT {pk}) FROM {table}` | = 0 | 告警 |
| DQ-003 | 及时性 | 当日数据量 > 前7日均值的 50% | `{sql}` | > 0 | 告警 |

---

## 7. 调度依赖 [必填]

### 7.1 上游依赖

| 依赖任务 | 依赖类型 | 说明 |
|----------|----------|------|
| {upstream_job} | 强依赖 / 弱依赖 | {说明} |

### 7.2 下游通知

| 下游任务 | 通知方式 | 说明 |
|----------|----------|------|
| {downstream_job} | 自动触发 / 通知负责人 | {说明} |

---

## 8. 非功能性需求 [可选]

| 类别 | 要求 |
|------|------|
| 性能 | 调度任务运行时长 < {N} 分钟 |
| 存储 | 预估数据量 {N} 行 / {N} GB，增长趋势 {描述} |
| 权限 | 读权限：{team/user}；写权限：{team/user} |
| 监控 | 失败告警：{email/webhook}；延迟告警：超 {N} 分钟 |

---

## 9. 待确认事项

- [ ] {问题 1}：{描述，等待谁确认}
- [ ] {问题 2}：{描述}

---

## 10. 变更记录 [可选]

| 版本 | 时间 | 变更内容 | 变更人 |
|------|------|----------|--------|
| v1.0 | {date} | 初始版本 | {name} |
